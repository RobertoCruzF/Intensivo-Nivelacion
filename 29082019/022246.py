from numpy import loadtxt


# ESTA PARTE DEL CODIGO NO FUNCIONA DADO QUE NO TENGO EL ARCHIVO QUE USAN EN EL VIDEO
# ES APLICAR LO APRENDIDO DURANTE EL VIDEO UTILIZANDO LOS DATOS DENTRO DEL ARCHIVO


#data=loadtxt('wind.data')

#dates=data[:,:3]

#winds=data[:,3:]

#print(winds.mean(), winds.min(), winds.max(), winds.std()) # promedio, minimo, maximo y desviacion estandar

#print(winds.min(axixs=0)) # columans

#print(winds.min(axixs=1)) #filas

#print(winds.argmax(axis=1))

#row=winds.max(axis=1).argmax()

#print(dates[row])


#print row

#print dates

#print dates[row,:]

#print wind.argmax()

#print wind.argmax() // winds.shape[1]

#print np.unravel_index(winds.argmax(),winds.shape)

#print winds==winds.max()


#print np.where(winds==winds.max())

#print np.nonzero(winds==winds.max())


# otra ventana

# import numpy as np

#row,col=np.where(winds==winds.max())

#row=winds.max(axis=1).argmax()

#print(dates[row])

#january=dates[:,1]==1

#print dates

#print(winds[january,:].mean(axis=0))

#print dates[:,1]
	
#print %run






