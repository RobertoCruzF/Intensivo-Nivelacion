g=9
h=8

# Imprime en consola "g is less than h" en caso de que  g es mayor a h
# en caso contrario si se cumple la condicion de que g es igual a h
# imprime en consola "g is equal to h", en caso d que no se cumpla la condicion
# anteriormente nombrada, imprime en consola print "g is greater than h"
if g>h:
	print "g is less than h"
else:
	if g==h:
		print "g is equal to h"
	else:
		print "g is greater than h"